# Consumers and adapters

A consumer copies `palette.json` from a Static Noise Git tag into its own repository. The consumer owns conversion into its target format, target compatibility, tests, provenance, and release schedule.

Static Noise does not know or validate consumer repositories. Keep the snapshot and its source tag/commit visible in the consumer's source tree.
