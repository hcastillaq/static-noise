# Token contract

`palette.json` is the canonical Static Noise contract. Tokens describe semantic roles, not configuration keys for any particular tool.

All color values are six-digit hexadecimal strings. `borderStrong` is the structural border role; `cyan` is reserved for active focus and interaction.
