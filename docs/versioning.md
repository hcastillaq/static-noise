# Versioning

Static Noise uses semantic versioning for the token contract. Consumers pin an immutable Git tag and record both the tag and commit SHA in their own repository.

Adding compatible optional tokens is a minor release. Removing or changing the meaning of an existing token is a major release. Documentation and validation-only fixes are patch releases.
