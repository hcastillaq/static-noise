# Legacy compatibility artifacts

The files in this directory are frozen compatibility snapshots for consumers that
are still migrating to their own adapter repositories. Static Noise no longer
generates, validates, synchronizes, or releases these files.

New integrations must consume a tagged `palette.json` snapshot and own their
conversion, tests, compatibility policy, and release lifecycle.
